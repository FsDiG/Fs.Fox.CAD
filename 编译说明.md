# Fs.Fox.CAD 构建与项目结构

本文面向仓库维护者，记录当前解决方案的构建目标、工具链、条件编译符号和输出规则。使用 NuGet 包开发业务插件时，通常只需阅读根目录 [README](README.md) 中的“选择和安装”部分。

## 构建原则

Fs.Fox.CAD 使用“共享源码 + 平台项目”的组织方式：

- `src/CADShared/CADShared.projitems` 保存主要实现。
- 每个 AutoCAD/ZWCAD API 代际使用独立项目绑定对应的厂商托管程序集。
- 各平台项目导入同一份共享源码，输出平台专用程序集。
- 不使用目标框架名称代替 CAD 版本；只有 API/二进制代际确实变化时才新增平台项目。

这种结构可以共享大部分业务封装，同时保留厂商程序集的真实类型身份。`Fs.Fox.AutoCad.dll` 与 `Fs.Fox.ZwCad.dll` 不能互换，同一系列不同 API 代际的产物也不应混用。

## 当前发布目标

`IFoxCAD.sln` 和 GitHub Actions 当前构建以下四组类库与测试项目，全部面向 Windows x64：

| 类库项目 | NuGet 包 | 目标框架 | 编译期 SDK/引用 | 条件编译符号 | 输出目录前缀 |
| --- | --- | --- | --- | --- | --- |
| `Fs.Fox.AutoCad2019` | `IFox.CAD.ACAD2019` | `net48` | `AutoCAD.NET` 23.0.0 | `ACAD;AC_2019;AC_NET48` | `AC_2019` |
| `Fs.Fox.AutoCad2025` | `IFox.CAD.ACAD2025` | `net8.0-windows7.0` | `AutoCAD.NET` 25.0.1 | `ACAD;AC_2025` | `AC_2025` |
| `Fs.Fox.ZwCad2022` | `IFox.CAD.ZCAD2022` | `net48` | ZRXSDK 2022 x64 / `ZWCAD.NetApi` 20.22.0 | `ZWCAD;ZW_2022` | `ZW_2022` |
| `Fs.Fox.ZwCad2025` | `IFox.CAD.ZCAD2025` | `net48` | `ZWCAD.NetApi` 20.25.0 | `ZWCAD;ZW_2025` | `ZW_2025` |

`$(Configuration)` 会由 MSBuild 自动追加为 `Debug` 或 `Release`。ZWCAD 2025 项目的构建产物同时用于 ZWCAD 2025 和 2026，不新增 `ZW_2026`；具体依据见 [ZWCAD 版本兼容性与迁移说明](docs/ZWCAD-version-compatibility.md)。

### 测试入口

| 测试项目 | 引用的类库项目 | 输出目录 |
| --- | --- | --- |
| `TestAcad2019` | `Fs.Fox.AutoCad2019` | `Build/AC_2019_<配置>/` |
| `TestAcad2025` | `Fs.Fox.AutoCad2025` | `Build/AC_2025_<配置>/` |
| `TestZcad2022` | `Fs.Fox.ZwCad2022` | `Build/ZW_2022_<配置>/` |
| `TestZcad2025` | `Fs.Fox.ZwCad2025` | `Build/ZW_2025_<配置>/` |

构建测试项目会同时构建对应类库，适合作为日常回归入口。

## 环境要求

- Windows x64。
- Visual Studio Build Tools/MSBuild，包含 .NET Framework 4.8 Developer Pack。
- .NET 8 SDK。
- 能还原项目声明的 NuGet 依赖。
- 构建 ZWCAD 2022 时，`$(FsLibsDir)SDK\ZRX2022\lib-x64\` 下需存在仓库项目文件列出的 ZRXSDK x64 托管程序集。

AutoCAD 及 ZWCAD 的宿主程序不是完成普通编译的必要条件，但真实加载、调试和运行时验收必须安装对应 CAD。

## 本地构建

在 Visual Studio Developer PowerShell 中，从仓库根目录执行：

```powershell
# .NET Framework 目标
msbuild .\tests\TestAcad2019\TestAcad2019.csproj '/t:Restore;Build' `
  /p:Configuration=Release '/p:Platform=Any CPU'

msbuild .\tests\TestZcad2022\TestZcad2022.csproj '/t:Restore;Build' `
  /p:Configuration=Release '/p:Platform=Any CPU'

msbuild .\tests\TestZcad2025\TestZcad2025.csproj '/t:Restore;Build' `
  /p:Configuration=Release '/p:Platform=Any CPU'

# .NET 8 目标
dotnet build .\tests\TestAcad2025\TestAcad2025.csproj -c Release
```

也可以在 Visual Studio 中打开 `IFoxCAD.sln` 构建当前四个目标。CI 的精确命令见 [构建检查工作流](.github/workflows/build-and-deploy.yml)。

## 输出目录

类库与对应测试程序集输出到同一个目录，且不追加目标框架子目录：

```text
Build/
  AC_2019_Debug/
  AC_2019_Release/
  AC_2025_Debug/
  AC_2025_Release/
  ZW_2022_Debug/
  ZW_2022_Release/
  ZW_2025_Debug/
  ZW_2025_Release/
```

例如，`Build/ZW_2025_Release/` 同时包含 `Fs.Fox.ZwCad.dll` 与 `TestZcad2025.dll`。测试宿主时应对测试入口程序集执行 `NETLOAD`，并确保同目录依赖能够被解析。

## 非当前发布目标

仓库中还保留两个未纳入 `IFoxCAD.sln` 和当前发布工作流的项目：

- `src/Fs.Fox.AutoCad2021`：`net48`，依赖仓库外部的 `AutoCAD.ManagedReferences.props`。
- `src/Fs.Fox.AutoCad2027`：当前按 `net8.0-windows7.0` 构建，并使用 ObjectARX 2025 managed reference 维持上层 .NET 8 产品链。详见 [AC_2027 .NET 8 兼容策略记录](docs/AC_2027-net8-compatibility-decision.md)。

这些项目存在于源码树中不代表它们会由当前标签发布流程生成 NuGet 包。

## 打包和发布

推送 `v*` 标签会触发 `.github/workflows/release.yml`，依次构建、打包并发布当前四个目标：

```powershell
git tag v1.2.3
git push origin v1.2.3
```

版本号由标签注入构建过程。发布前应先确认日常构建检查通过，并完成目标 CAD 宿主验收。完整流程与所需 Secrets 见 [NuGet 发布工作流说明](.github/workflows/release.md)。

## 验证边界

自动构建和打包能够验证：

- 共享源码可在各目标项目中编译。
- NuGet/SDK 引用能够解析。
- 类库和测试程序集可以生成。

它们不能验证 CAD 宿主中的程序集绑定、`NETLOAD`、命令注册、数据库行为、WPF/WinForms/Ribbon 入口或自动加载。涉及新宿主版本、SDK 代际或部署方式时，真实 CAD 验收仍是发布门槛。
